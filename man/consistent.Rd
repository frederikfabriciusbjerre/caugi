% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints-solver.R
\name{consistent}
\alias{consistent}
\title{Check whether a constraint set is satisfiable by some graph of the
chosen class.}
\usage{
consistent(ctr, nodes, class = "DAG")
}
\arguments{
\item{ctr}{A \code{caugi_constraints} object.}

\item{nodes}{Character vector of node names.}

\item{class}{One of \code{"DAG"}, \code{"UG"}, \code{"PDAG"}, \code{"ADMG"}. Defaults
to \code{"DAG"}.}
}
\value{
\code{TRUE} if \code{ctr} is satisfiable over \code{nodes}, \code{FALSE}
otherwise.
}
\description{
Returns \code{TRUE} if there exists a graph of class \code{class} over \code{nodes}
that satisfies every formula in \code{ctr}, \code{FALSE} otherwise. Powered
by splr behind the scenes.

Supported atom surface depends on the class:
\itemize{
\item \code{"DAG"}: \verb{-->} edges only; \code{parents}, \code{children},
\code{neighbors}, \code{ancestors}, \code{descendants}.
\item \code{"UG"}: \verb{---} edges only; \code{neighbors}.
\item \code{"PDAG"}: \verb{-->} and \verb{---} edges; \code{parents}, \code{children},
\code{neighbors}, \code{ancestors}, \code{descendants}.
\item \code{"ADMG"}: \verb{-->} and \verb{<->} edges; \code{parents}, \code{children},
\code{spouses}, \code{neighbors}, \code{ancestors}, \code{descendants}.
}
Common to all: \code{acyclic()}, \code{collider()}, \code{v_structure()},
cardinality, quantifiers, boolean combinators. Tier-C atoms
(\code{dsep}) are evaluator-only.
}
\examples{
ctr <- caugi_constraints(A \%-->\% B, !(B \%-->\% A))
consistent(ctr, nodes = c("A", "B"))
consistent(
  caugi_constraints(A \%---\% B),
  nodes = c("A", "B"),
  class = "UG"
)

}
\seealso{
Other constraints: 
\code{\link{caugi_constraints}()},
\code{\link{caugi_constraints_class}()},
\code{\link{caugi_predicate}()},
\code{\link{constraints}()},
\code{\link{enumerate}()},
\code{\link{satisfies}()},
\code{\link{violations}()},
\code{\link{with_constraints}()}
}
\concept{constraints}
