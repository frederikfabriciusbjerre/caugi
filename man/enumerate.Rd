% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints-solver.R
\name{enumerate}
\alias{enumerate}
\title{Enumerate graphs satisfying a constraint set.}
\usage{
enumerate(ctr, nodes, class = "DAG", limit = 100L)
}
\arguments{
\item{ctr}{A \code{caugi_constraints} object.}

\item{nodes}{Character vector of node names.}

\item{class}{One of \code{"DAG"}, \code{"UG"}, \code{"PDAG"}, \code{"ADMG"}. Defaults
to \code{"DAG"}.}

\item{limit}{Integer; maximum number of graphs to return. Defaults
to \code{100}.}
}
\value{
A list of \code{caugi} objects (each carrying the requested
\code{class}).
}
\description{
Returns up to \code{limit} distinct graphs of the chosen class over
\code{nodes} that satisfy every formula in \code{ctr}. Each result is a
\code{caugi} object. With no constraints and \code{class = "DAG"},
\code{enumerate()} produces all unique DAGs on \code{nodes} (the Robinson
sequence: 1, 3, 25, 543, … for n = 1, 2, 3, 4, …).
}
\examples{
# All DAGs on 3 nodes — 25.
length(enumerate(caugi_constraints(), c("A", "B", "C"), limit = 200))

# All UGs on 3 nodes — 2^3 = 8 (one var per unordered pair).
length(enumerate(caugi_constraints(), c("A", "B", "C"),
                 class = "UG", limit = 50))

}
\seealso{
Other constraints: 
\code{\link{caugi_constraints}()},
\code{\link{caugi_constraints_class}()},
\code{\link{caugi_predicate}()},
\code{\link{consistent}()},
\code{\link{constraints}()},
\code{\link{satisfies}()},
\code{\link{violations}()},
\code{\link{with_constraints}()}
}
\concept{constraints}
