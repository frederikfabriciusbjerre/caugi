% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{.canonical_edge_order}
\alias{.canonical_edge_order}
\title{Canonicalise symmetric edges for comparison}
\usage{
.canonical_edge_order(e)
}
\arguments{
\item{e}{A \code{data.table} of edges with \code{from}, \code{edge} and \code{to} columns.}
}
\value{
\code{e} with symmetric edges oriented canonically.
}
\description{
A symmetric glyph denotes the same edge either way round, so \code{A --- B} and
\code{B --- A} must compare equal. Orients every symmetric edge so that \code{from}
sorts before \code{to}, leaving asymmetric edges alone.

Which glyphs are symmetric is read from the edge registry rather than
hard-coded, so custom edges added with \code{\link[=register_caugi_edge]{register_caugi_edge()}} are handled
too.
}
\keyword{internal}
